# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=forgeindex
POSTGRES_PASSWORD=forgeindex_secret
POSTGRES_DB=forgeindex

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=forgeindex
MINIO_SECURE=false

# Apify (optional — falls back to yt-dlp search)
APIFY_API_TOKEN=

# Google Gemini (annotation LLM + GDPR checks + query parsing)
GOOGLE_API_KEY=

# Anthropic (optional)
ANTHROPIC_API_KEY=

# API
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# ML Models
CLIP_MODEL=ViT-B-32
CLIP_PRETRAINED=openai
YOLO_MODEL=yolov8n.pt
WHISPER_MODEL=base

# Pipeline
MAX_WORKERS=4
KEYFRAME_INTERVAL=30
SCENE_THRESHOLD=0.3
DEDUP_THRESHOLD=0.95
YOLO_CONFIDENCE=0.3
YOLO_BATCH_SIZE=16
MIN_ACTIVITY_DURATION=2.0
VIDEO_MAX_RESOLUTION=720
